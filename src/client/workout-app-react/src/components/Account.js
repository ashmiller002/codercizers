function Account() {
    return(
        <div>account</div>
        //add a disable button which automatically logs you out and sets fullUserInfo.userStatus to "disable"
    )
}

export default Account;